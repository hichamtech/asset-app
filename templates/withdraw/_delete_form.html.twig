<form method="post" action="{{ path('withdraw_delete', {'id': withdraw.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ withdraw.id) }}">
    <button class="btn">Delete</button>
</form>
